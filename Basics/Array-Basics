const firstname="chandan"
const secondname="Krishnani"
const val=42
const arr=[
    'string',
    42,
    function(){console.log('hi')},

]
console.log(firstname,secondname)

for (let i=0;i<arr.length;i++)
    console.log(arr[i])
arr[2]();
